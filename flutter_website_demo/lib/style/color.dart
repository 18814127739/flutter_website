import 'dart:ui';

// 产品颜色
class ProductColors {
  static const Color bgColor = Color(0xffffffff);

  static const divideLineColor = Color.fromRGBO(245, 245, 245, 1);

  static const typeColor = Color.fromRGBO(182, 9, 9, 1);

  static const pointColor = Color.fromRGBO(132, 95, 63, 1);
}